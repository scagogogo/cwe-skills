version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell
    - gofmt

formatters:
  enable:
    - gofmt

exclusions:
  rules:
    - path: _test\.go
      linters:
        - errcheck
